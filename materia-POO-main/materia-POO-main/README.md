 ¿Cuál es la diferencia entre la clase CuentaBancaria y un objeto CuentaBancaria?
  R: la diferencia es que en la clase CuentaBancaria es la plantilla que define donde van los atributos y cuales son los metodos pero es el objeto es que ya contiene todos los datos especificos del usuario

¿Qué hace exactamente la palabra new?
 R: lo que hace es crear elobjeto o el metodo para que funcione de verdad

Si tienes dos cuentas a y b y ejecutas a.consignar(1000), ¿cambia el saldo de b? Justifica tu respuesta.
R: no, porque aunque tengan los mismos metodos son clases distintas y lo que hace una no afecta a la otra

¿Por qué se dice que el método retirar protege el estado del objeto?
 R: porqué no actua sin una validacion, asi que evita que se cambien o modifiquen datos de forma extraña como que de un valor negativo

  Propón una entidad distinta a las trabajadas e indica dos de sus atributos y dos de sus métodos.
  R: entidad: estudiante
     atributos: String nombre
                int notas
     metodos: notaFinal() 
              clasificacionSegunPromedio()
              
  [build.xml](https://github.com/user-attachments/files/31714435/build.xml)
